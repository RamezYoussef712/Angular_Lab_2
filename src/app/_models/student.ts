export class Student {
    id: number;
    name: string;
    age: number;
    dept_no: number;

    constructor(id: number, name: string, age: number, dept_no: number) {
        this.id = id;
        this.name = name;
        this.age = age;
        this.dept_no = dept_no;
    }
}
